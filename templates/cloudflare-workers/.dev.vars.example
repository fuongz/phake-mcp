# Cloudflare Workers MCP Server

# KV Namespace - create via: wrangler kv namespace create TOKENS
RS_TOKENS_ENC_KEY=

# Optional: OAuth client (if using OAuth)
# OAUTH_CLIENT_ID=
# OAUTH_CLIENT_SECRET=

# Optional: Auth strategy (oauth, bearer, api_key, custom, none)
# AUTH_STRATEGY=oauth